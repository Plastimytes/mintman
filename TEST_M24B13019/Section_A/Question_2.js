//A program to store the name of a country in Africa, it's population and the current year

let Country_name="Uganda";
var population= 5600000;//Populaton is stored as a variable 
const current_year= 2024;//The current year is stored as a constant

console.log(Country_name, "has a population of", population, "in",current_year);